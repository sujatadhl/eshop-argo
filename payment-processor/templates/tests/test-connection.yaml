apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "payment-processor.fullname" . }}-test-connection"
  labels:
    {{- include "payment-processor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "payment-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
