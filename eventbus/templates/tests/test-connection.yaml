apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eventbus.fullname" . }}-test-connection"
  labels:
    {{- include "eventbus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eventbus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
