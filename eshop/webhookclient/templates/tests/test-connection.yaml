apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webhookclient.fullname" . }}-test-connection"
  labels:
    {{- include "webhookclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webhookclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
