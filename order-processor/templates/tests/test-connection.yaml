apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "order-processor.fullname" . }}-test-connection"
  labels:
    {{- include "order-processor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "order-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
